# Change this line to use the environment variable Render provides
server.port=${PORT:8080}

# Keep your other settings
spring.application.name=mongodbexample

# FIXED: Ensure these are on two different lines
spring.data.mongodb.uri=mongodb+srv://subharthykuiry_db_user:1234@cluster0.ipsyvj0.mongodb.net/inventory_db
spring.data.mongodb.database=inventory_db

# This will show you EXACTLY why the server is shutting down in the Render logs
logging.level.org.springframework=DEBUG
logging.level.org.mongodb.driver=DEBUG